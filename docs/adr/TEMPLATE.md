<!--
Copy to NNNN-slug.md, numbered in the order records are accepted and never
reused. A changed decision is a new record that supersedes the old one: point
the old record's status at the new one rather than editing its decision. Keep
only sections that earn their place. Delete this comment.
-->

# NNNN — Title

**Status:** proposed | accepted | superseded by [NNNN](NNNN-slug.md) | deprecated
**Date:** YYYY-MM-DD

## Context

The forces that make this a decision rather than a default: the constraint, the
failure mode, or the question the code cannot answer on its own. State the
facts; leave the conclusion to the next section.

## Decision

What was chosen, in the present tense, so the sentence stays true after the
code moves. Name the module, seam, or contract the decision lands in.

## Alternatives considered

The options that lost, one line each, with the reason. This is what a reader
reconsidering the decision needs first. Drop the section when there was only
one option.

## Consequences

What it makes easy, what it rules out, and the cost accepted — not only the
benefit. End with the condition that would reopen it: "revisit if ...".

## Related

- The [engineering log](../engineering-log.md#anchor) entry that produced it.
- The document, unit, or ADR this extends or supersedes.
