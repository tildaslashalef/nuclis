# Fixture provenance

Everything in this directory is **committed evidence**: outputs of running the
pinned reference at a known revision against the pinned model artifact. It is
the oracle that `make gate NAME='qwen38-trace-*'` and the benchmark records are checked against.
The regenerable parts of the reference pipeline (the llama.cpp checkout, live
run outputs under `.zig-cache/`) stay out of git and are rebuilt on demand by
[../../docs/reference/reference-baseline.md](../../docs/reference/reference-baseline.md).

| Path | What it is | Produced by |
| --- | --- | --- |
| `model.sha256` | SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482` of the pinned `Qwen3.8-27B-UD-Q4_K_M.gguf` under `~/.nuclis/models/qwen/` | captured at baseline time (2026-09-06) |
| `tokenizer-metadata.json` | vocabulary facts asserted by the `qwen38-vocabulary` gate | captured at baseline time from the pinned artifact |
| `reference-hello-comma/` | llama.cpp layer/logit traces for the `'Hello,'` single-token comparison (129 `.f32` files; the slices `compare-generation.py` reads at `--positions 2`) | `scripts/reference-generation.cpp` against llama.cpp `7620399f58aebfd2196b74021f9581bcf7218cb9`, 2026-09-06 |
| `reference-chat/` | multi-turn chat trace, wider oracle for session/prefix work | same revision and artifact, 2026-09-06 |
| `gemma4-hello-comma/` | llama.cpp layer/logit traces for Gemma 4 12B (`unsloth/gemma-4-12b-it-GGUF` `gemma-4-12b-it-UD-Q4_K_XL.gguf`, SHA-256 `90fd944d…`) on the prompt `<bos>Hello,` (`prompt-tokens.json` = `[2, 9259, 236764]`): 144 layer files (three positions × 48 layers, 3840 floats each) and `logits.f32` (262,144 floats); the payload of `make gate NAME='gemma4-trace-f*'` | the same `scripts/reference-generation.cpp` (generalized to any F32 width) against llama.cpp `7620399f58aebfd2196b74021f9581bcf7218cb9`, Metal, F32 cache, 2026-09-11 |
| `gemma4-qat-hello-comma/` | the same traces for the catalogue's QAT file (`unsloth/gemma-4-12B-it-qat-GGUF` `gemma-4-12B-it-qat-UD-Q4_K_XL.gguf`, SHA-256 `90fd44e2…`, every matrix Q4_0) on the same prompt and ids; the payload of the `gemma4-qat-trace-*` gates (the K-quant traces above serve `gemma4-trace-*`) | the same harness against the same llama.cpp revision, Metal, F32 cache, 2026-09-12 (MODL-08) |
| `gemma4-26b-a4b-hello-comma/` | the same traces for the catalogue's `gemma-4-26b-a4b` (`unsloth/gemma-4-26B-A4B-it-qat-GGUF` `gemma-4-26B-A4B-it-qat-UD-Q4_K_XL.gguf`, SHA-256 `a7c5bc71…`, the mixture of experts, every matrix Q4_0) on the same prompt and ids: 90 layer files (three positions × 30 layers, 2,816 floats each) and `logits.f32`; the payload of `make gate NAME=gemma4-26b-a4b-trace-cpu` | the same harness against the same llama.cpp revision, Metal, F32 cache, 2026-09-18 (MODL-09) |
| `muse-glimmer-hello-comma/` | the same traces for `muse-glimmer-30b` (`unsloth/Muse-Glimmer-30B-GGUF` `Muse-Glimmer-30B-UD-Q4_K_XL.gguf`, SHA-256 `82bece30…`) on `<|begin_of_text|>Hello,` (`prompt-tokens.json` = `[200000, 19873, 24]`): 156 layer files (three positions × 52 layers, 6,656 floats each) and `logits.f32` (202,048 floats; greedy token 372); the payload of `make gate NAME=muse-trace-cpu` (MODL-11) | the same harness against llama.cpp `7620399f58aebfd2196b74021f9581bcf7218cb9`, Metal, F32 cache, 2026-09-19 ([muse-glimmer.md](../../docs/reference/muse-glimmer.md#cpu-reference-against-the-oracle-modl-11-2026-09-19)) |
| `run-2026-09-06/` | raw baseline run records: harness, prompt files (512/4,096/16,384/32,640 tokens), responses, checksums. The `prompt-*.json` token arrays are also the inputs of nuclis's own acceptance runs (`scripts/nuclis-baseline.py` → `bench --prompt-tokens`), so both sides measured the same tokens | `scripts/reference-baseline.py` run, 2026-09-06; summarized in [../../docs/benchmarks/reference-2026-09-06.json](../../docs/benchmarks/reference-2026-09-06.json) |
| `run-2026-09-12-gemma4/` | the same harness on Gemma 4 12B (`gemma-4-12b-it-UD-Q4_K_XL.gguf`, SHA-256 `90fd944d…`, `--family gemma4`): prompt arrays for 512/4,096/16,384/32,639 tokens (each starting with `<bos>` 2, then the Gemma template's `<|turn>user` prefix, the synthetic corpus, and the suffix ending in the pre-closed thought channel), responses, checksums, summary. The inputs of `make workload NAME=gemma4-12b/acceptance` (the K-quant entry) | `scripts/reference-baseline.py --family gemma4 --prompt-lengths 512,4096,16384,32639`, 2026-09-12, server at 32,768 context with the reference recipe's flags; summarized in [../../docs/benchmarks/reference-2026-09-12-gemma4.json](../../docs/benchmarks/reference-2026-09-12-gemma4.json) |
| `run-2026-09-12-gemma4-qat/` | the same harness on the catalogue's QAT Gemma 4 12B file (`gemma-4-12B-it-qat-UD-Q4_K_XL.gguf`, SHA-256 `90fd44e2…`, `--family gemma4`): the same four prompt lengths and construction (identical token arrays, since both files share the vocabulary and template), its own responses, checksums, and summary. The inputs of `make workload NAME=gemma4-qat/acceptance` (`baseline-gemma4` uses the directory above) | `scripts/reference-baseline.py --family gemma4 --prompt-lengths 512,4096,16384,32639`, 2026-09-12 (MODL-08), server at 32,768 context with the reference recipe's flags; summarized in [../../docs/benchmarks/reference-2026-09-12-gemma4-qat.json](../../docs/benchmarks/reference-2026-09-12-gemma4-qat.json) |
| `run-2026-09-18-gemma4-26b-a4b/` | the same harness on the catalogue's `gemma-4-26b-a4b` (`gemma-4-26B-A4B-it-qat-UD-Q4_K_XL.gguf`, SHA-256 `a7c5bc71…`, the mixture of experts, `--family gemma4`): the same four prompt lengths and construction (token arrays byte-identical to the two 12B runs, since the three files share the vocabulary and template), its own responses, checksums, and summary. The inputs of `make workload NAME=gemma4-26b-a4b/acceptance` | `scripts/reference-baseline.py --family gemma4 --prompt-lengths 512,4096,16384,32639`, 2026-09-18 (MODL-10), server at 32,768 context with the reference recipe's flags; summarized in [../../docs/benchmarks/reference-2026-09-18-gemma4-26b-a4b.json](../../docs/benchmarks/reference-2026-09-18-gemma4-26b-a4b.json) |
| `run-2026-09-19-muse-glimmer/` | the same harness on the catalogue's `muse-glimmer-30b` (`Muse-Glimmer-30B-UD-Q4_K_XL.gguf`, SHA-256 `82bece30…`, `--family muse-glimmer`): the same four prompt lengths and construction (each array starting with `<|begin_of_text|>` 200000, then the template's synthesized system turn at its default `Reasoning strength: high.` with the server's `Current date: 2026-09-19.` line, the `<|start|>user` prefix, the synthetic corpus, and the suffix ending at `<|start|>assistant`), its own responses, checksums, and summary. The inputs of `make workload NAME=muse/acceptance` | `scripts/reference-baseline.py --family muse-glimmer --prompt-lengths 512,4096,16384,32639`, 2026-09-19 (MODL-13), server at 32,768 context with the reference recipe's flags; summarized in [../../docs/benchmarks/reference-2026-09-19-muse-glimmer.json](../../docs/benchmarks/reference-2026-09-19-muse-glimmer.json) |
| `bonsai-hello-comma/` | the same traces for `bonsai-2-27b` from its bring-up file (`prism-ml/Ternary-Bonsai-2-27B-gguf` `Ternary-Bonsai-2-27B-PQ2_0.gguf`, SHA-256 `3907dc16…`, every matrix PQ2_0 in a Hadamard-rotated basis; the catalogue's PTQ1_0 file, the same weights in the denser packing, matches them at the same thresholds) on `Hello,` (`[9419, 11]`, Qwen3.8's tokens): 128 layer files (two positions × 64 layers, 5,120 floats) and `logits.f32` (248,320 floats; greedy token 353, as Qwen3.8's); the payload of `make gate NAME=bonsai-trace-cpu` (MODL-16 session 2) | the same harness built against the **PrismML fork** `5d80cff0b8cb9f2bf823cfc4e71e3abb97f290d6` (release `prism-b10687-5d80cff`; the only decoder of the file's encodings), Metal, F32 cache, 2026-09-18 ([bonsai.md](../../docs/reference/bonsai.md#oracle-the-prismml-fork)) |
| `run-2026-09-18-bonsai/` | the workload harness against the PrismML fork's server (`prism-b10687-5d80cff`, the only decoder of the file) holding `bonsai-2-27b`'s bring-up file (`Ternary-Bonsai-2-27B-PQ2_0.gguf`, SHA-256 `3907dc16…`, the entry's file at the time; the entry moved to the PTQ1_0 packing the same day, [bench.md](../../docs/reference/bench.md#bonsai-2-27b-acceptance-record-modl-17-2026-09-18)) with `--family qwen38 --reference-revision 5d80cff0…`: the same four prompt lengths and construction, its own responses, checksums, and summary. The token arrays are byte-identical to `run-2026-09-06`'s and `boundary-2026-09-06`'s (the file shares Qwen3.8's vocabulary and the single-user-turn rendering of its template; verified by `cmp` on 2026-09-18), and `make workload NAME=bonsai/acceptance` feeds this run's arrays through `bench --prompt-tokens` against its records | `scripts/reference-baseline.py` run, 2026-09-18; summarized in [../../docs/benchmarks/reference-2026-09-18-bonsai.json](../../docs/benchmarks/reference-2026-09-18-bonsai.json) |
| `perplexity/` | pinned `llama-perplexity` results on wikitext-2-raw's `wiki.test.raw` (SHA-256 `173c87a5…`, fetched by `make eval-corpus`, never committed), eight windows of 512, per-token decode (`-b 512 -ub 1`): `qwen38-` (Qwen3.8-27B UD-Q4_K_M), `gemma4-` (Gemma 4 12B UD-Q4_K_XL), `gemma4-26b-a4b-` (its QAT UD-Q4_K_XL, with a 1 % `tolerance` and its reason), and `muse-glimmer-wikitext2-c512x8.json`; each records the flags, the text's digest, the perplexity, its error, and the running estimates. The payload of `make gate NAME='*-perplexity'`. The `*-c4096x4.json` pair (Gemma 4 12B and 26B-A4B, four windows of 4,096, `-b 4096 -ub 1`) is the long tier's: the 12B's feeds `gemma4-perplexity-4k`, the 26B's is the evidence of an open −1.79 % difference ([eval.md § The long-context record](../../docs/reference/eval.md#the-long-context-record-2026-09-24)) | `scripts/reference-perplexity.py --ubatch 1` against llama.cpp `7620399f58aebfd2196b74021f9581bcf7218cb9`, Metal, 2026-09-24 (APPS-14, [eval.md](../../docs/reference/eval.md)) |
| `boundary-2026-09-06/` | context-boundary run records (32,767/32,768 positions); `prompt-32639.json` is the 32K acceptance input | same run; summarized in [../../docs/benchmarks/reference-boundary-2026-09-06.json](../../docs/benchmarks/reference-boundary-2026-09-06.json) |

Rules for agents:

- **Never edit or regenerate these files in place.** They are the fixed point
  that proves kernel changes are equivalent. If a trace must be re-produced
  (e.g. a new reference revision), generate it under `.zig-cache/reference/`
  first, verify it byte-for-byte against these fixtures, and only then propose
  a fixture update as its own reviewed commit.
- The acceptance value of `make gate NAME='qwen38-trace-*'` is max abs `0.0001220703125` over
  129 files (see [../../docs/reference/bench.md](../../docs/reference/bench.md)).
  No threshold is loosened without a documented reason.
- `reference-chat/` is an opt-in oracle (session/prefix comparisons); the
  default `make gate NAME='qwen38-trace-*'` payload is only `reference-hello-comma/`.
